import { AutofocusnextinputDirective } from './autofocusnextinput.directive';

describe('AutofocusnextinputDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofocusnextinputDirective();
    expect(directive).toBeTruthy();
  });
});
