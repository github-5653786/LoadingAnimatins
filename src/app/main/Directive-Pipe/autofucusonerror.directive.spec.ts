import { AutofucusonerrorDirective } from './autofucusonerror.directive';

describe('AutofucusonerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AutofucusonerrorDirective();
    expect(directive).toBeTruthy();
  });
});
